# pipevn
# https://github.com/amjith/fuzzyfinder
class MachineFinder():
    @staticmethod
    def find(machines, pattern=None):
        if pattern is None or not pattern:
            pattern = '*'

        return []
